# Этап 2 — воспроизведение, 22.09.2026

Результат: **PASS проверенных сценариев; полная приёмка не завершена**. Прогон ограничен пользовательским бюджетом 10 минут, начало 18:48:14 UTC. UI-проверки закончены до 18:57 UTC. Код, APK и данные приложения не очищались и не переустанавливались.

Среда: Pixel_9 AVD API 37, `emulator-5554`, production `com.prod.mpod`, 1.0.18 (19), ранее установленный кандидат. Физический телефон не использовался. Повторная сборка и автоматические тесты не запускались.

Evidence: `build/qa-stage2-20260922/`. Состояние проверялось через UI и Android MediaSession; фактический звук на слух не проверялся. PASS воспроизведения означает корректные состояния плеера и движение позиции, а не отдельную акустическую проверку.

## Выполнено

| Сценарий | Наблюдение | Evidence |
|---|---|---|
| Добавить обычный внешний выпуск и запустить | Planet Money — The Domino’s Pizza Tracker Theory of Everything добавлен в очередь; PLAYING, позиция растёт, error=null | real-queue.xml, real-playing.xml, real-playing-media.txt |
| Пауза и перемотка +15/−10 | PAUSED; суммарное изменение позиции +5 секунд, до 8542 мс | paused-seek.xml, paused-seek-media.txt |
| Скорость 1.5× | UI показывает 1.5; MediaSession при воспроизведении speed=1.5 | speed-set.xml, background-notification-media.txt |
| Фон и уведомление | После Home воспроизведение продолжается; уведомление показывает нужный выпуск и Pause; Pause из уведомления останавливает на 238654 мс | notification.png, background-notification-media.txt, notification-paused-return-media.txt |
| Перезапуск процесса после паузы | Тот же выпуск, позиция 238654 мс (3:58), скорость 1.5×; без автозапуска | restored-player.xml, restored-player-media.txt |
| Скачанный выпуск без сети после перезапуска | Wi-Fi и mobile data отключены; Active default network: none. Выпуск запущен с сохранённой позиции, PLAYING, позиция выросла до 263187 мс, error=null | offline-connectivity.txt, offline-player.xml, offline-playing-media.txt |
| Удаление активного выпуска из очереди после паузы | MediaSession сброшена в NONE; затем новая очередь показывает первый выпуск без автозапуска | aliases-selected-media.txt, short-queue.xml |
| Естественное завершение и автопереход | Два 30-секундных QA aliases выпуска: episode 4 завершился, episode 3 автоматически запустился; очередь сократилась до одного | short-first-playing.xml, short-auto-next.xml, short-auto-next-media.txt |
| Конец последнего выпуска | NONE, пустая очередь и отсутствие старой карточки; оба выпуска отмечены прослушанными — UI предлагает Mark … as unlistened | empty-final.xml, empty-final-media.txt, listened-visible.xml |

Для короткой очереди использованы подготовленные локальные WAV, для основной проверки — настоящий внешний выпуск. Удаление локальных файлов напрямую не проверялось: исчезновение Downloaded в UI не доказывает физический cleanup.

## Не проверено в этом прогоне

- Перетаскивание очереди и абсолютная перемотка ползунком; пограничные перемотки.
- Ошибка аудиопотока и Retry; потеря сети именно при потоковом воспроизведении.
- Экран блокировки, audio focus, Bluetooth/наушники, длительное фоновое воспроизведение.
- Остановка процесса во время PLAYING (проверялся перезапуск после паузы).
- Остальные скорости, прямые Room/files-инварианты, полный автоматический gate.

Эти пункты не считаются PASS. В выполненных сценариях blocker не обнаружен.

## Состояние после прогона

Wi-Fi/mobile data восстановлены в исходное включённое состояние (1/1). Воспроизведение остановлено естественно, очередь пуста. Planet Money сохраняет прогресс примерно 4:25; QA aliases 4 и 3 прослушаны. Скорость оставлена 1.5×, фильтр подписок — Show all. Временный QA-сервер этого прогона остановлен, reverse tcp:8765 снят; для повторного обращения к локальным QA-лентам сервер потребуется запустить. AVD оставлен работающим.

`background-notification.xml` исключён из evidence: uiautomator не смог получить idle, файл оказался устаревшим. Уведомление подтверждено screenshot и MediaSession. `short-queue-finished.xml` снят ещё до завершения второго выпуска; доказательство пустой очереди — только `empty-final.xml`.
